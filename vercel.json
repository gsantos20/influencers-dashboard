{
  "version": 2,
  "buildCommand": "ng build --output-hashing=all --configuration production",
  "builds": [
    {
      "src": "dist/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "(.*)",
      "dest": "/index.html"
    }
  ]
}
